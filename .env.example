# Max raw diff length in chars for diff-only mode.
# If diff exceeds limit — repomix is used.
GITSCRIBE_DIFF_MAX_SIZE=5000

# Truncate diff to this many chars before sending to AI.
# Works in both modes (diff-only and repomix).
GITSCRIBE_DIFF_TRUNCATE=8000
